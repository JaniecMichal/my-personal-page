import { Navbar } from "../navbar";
import { Socials } from "../socials";

export const Header = () => {
	return (
		<header className="flex flex-row items-center justify-center px-8 py-6">
			<Navbar />
		</header>
	);
};
