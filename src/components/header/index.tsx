import { Navbar } from "../navbar"

export const Header = () => {
  return (
    <header className="sticky top-0 z-50 flex flex-row items-center justify-center px-8 pt-6">
      <Navbar />
    </header>
  )
}

